import { describe, it } from 'vitest';

/**
 * Integration tests for Office Editing flow
 * 
 * TODO: Update these tests to work with database instead of in-memory store
 */
describe('Office Editing Flow Integration', () => {
  it.skip('TODO: should preserve data when editing office details', async () => {
    // Test needs to be updated for database
  });

  it.skip('TODO: should regenerate schedules after provider changes', async () => {
    // Test needs to be updated for database
  });
});
