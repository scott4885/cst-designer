"use client";

/**
 * ThemeToggle — DEPRECATED / INERT
 * ────────────────────────────────
 * The CST Designer app is light-only by product decision. This component
 * used to cycle light/dark/system via next-themes; that behavior has been
 * removed. It is retained only so any dynamic import paths keep resolving,
 * and renders nothing. Safe to delete once no imports reference it.
 */
export function ThemeToggle() {
  return null;
}
