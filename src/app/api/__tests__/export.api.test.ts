import { describe, it } from 'vitest';

/**
 * API tests for /api/offices/[id]/export
 * 
 * TODO: Update these tests to work with database instead of in-memory store
 */
describe('POST /api/offices/[id]/export', () => {
  it.skip('TODO: should export schedule from database', async () => {
    // Test needs to be updated for database
  });
});
