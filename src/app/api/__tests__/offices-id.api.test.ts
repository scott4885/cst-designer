import { describe, it } from 'vitest';

/**
 * API tests for /api/offices/[id]
 * 
 * TODO: Update these tests to work with database instead of in-memory store
 */
describe('GET /api/offices/[id]', () => {
  it.skip('TODO: should get office by ID from database', async () => {
    // Test needs to be updated for database
  });
});

describe('PUT /api/offices/[id]', () => {
  it.skip('TODO: should update office in database', async () => {
    // Test needs to be updated for database
  });
});

describe('DELETE /api/offices/[id]', () => {
  it.skip('TODO: should delete office from database', async () => {
    // Test needs to be updated for database
  });
});
