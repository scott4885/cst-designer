# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.log

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
.env.local
.env.production
.env.*.local
# but keep the example template
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# prisma
prisma/*.db

# SQLite data
data/*.db
data/*.db-journal
data/

# Local Netlify folder (deprecated — no longer a deploy target)
.netlify

# OpenNext / Cloudflare build artifacts (deprecated adapter, kept out of git)
.open-next/
.wrangler/

# Visual QA screenshots (Phase 4 QA — not tracked in git)
.cst-rebuild-v3/logs/visual-qa/

# Phase 6 live-smoke screenshots + scratch spec + scratch config (not tracked)
.cst-rebuild-v3/logs/phase-6-screenshots/
e2e/qa-phase6-live-smoke.spec.ts
playwright.phase6.config.ts

# Playwright run artifacts + scratch screenshots
/playwright-report/
smoke-passed.png
